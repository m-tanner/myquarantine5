schemaVersion: '2.0.0'
commandTests:
  - name: "python installation"
    command: "which"
    args: ["python"]
    expectedOutput: ["/usr/local/bin/python\n"]
# TODO: This works, but needs authentication to pass
#  - name: "pytest"
#    command: "pytest"
#    args: ["tests/", "-s", "-v", "--disable-pytest-warnings"]